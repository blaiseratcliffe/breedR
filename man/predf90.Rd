% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genomic.R
\name{predf90}
\alias{predf90}
\title{Predict direct genomic values for new animals}
\usage{
predf90(
  snp_file,
  use_mu_hat = TRUE,
  acc = FALSE,
  acc_type = 1,
  use_diagG_acc = FALSE,
  outfile = "SNP_predictions",
  dir = tempdir()
)
}
\arguments{
\item{snp_file}{character. Path to genotype file for animals to predict,
in BLUPF90 format (see \code{\link{write_snp_file}}).}

\item{use_mu_hat}{logical. Add the base (mu_hat) to DGV so values are
comparable to GEBV (default TRUE).}

\item{acc}{logical. Compute reliability of predictions (default FALSE).
Requires \code{OPTION snp_p_value} in BLUPF90+ and
\code{OPTION snp_var} in PostGSF90.}

\item{acc_type}{numeric. 1.0 for dairy cattle (reliability) or 0.5 for
beef cattle (BIF accuracy). Default 1.0.}

\item{use_diagG_acc}{logical. Use inbreeding from G in the reliability
denominator (default FALSE).}

\item{outfile}{character. Name of the output file (default
"SNP_predictions").}

\item{dir}{character. Working directory containing PostGSF90 output files
(snp_pred). Default \code{tempdir()}.}
}
\value{
A data.frame with columns: id, call_rate, dgv, and optionally
  reliability.
}
\description{
Uses SNP effects from \code{\link{postgsf90}} to predict direct genomic
values (DGV) for animals not in the original evaluation. This enables
genomic prediction for young/new animals based only on their genotypes.
}
\details{
The prediction is: DGV = mu_hat + Z * a_hat, where a_hat are SNP effects
from PostGSF90 and Z is the centered genotype matrix.

PREDF90 must be run in the same directory where PostGSF90 was run, since
it reads the \code{snp_pred} file produced by PostGSF90.
}
