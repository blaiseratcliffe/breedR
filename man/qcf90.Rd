% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{qcf90}
\alias{qcf90}
\title{Quality control for genotype and pedigree data}
\usage{
qcf90(
  snp_file,
  ped_file = NULL,
  map_file = NULL,
  call_rate_markers = 0.9,
  call_rate_animals = 0.9,
  maf = 0.05,
  hwe = NULL,
  check_parentage = NULL,
  trio = FALSE,
  check_identity = FALSE,
  identity_threshold = 0.99,
  sex_chr = NULL,
  skip_chr = NULL,
  exclude_chr = NULL,
  save_clean = TRUE,
  save_log = TRUE,
  outcallrate = FALSE,
  remove_markers = TRUE,
  remove_animals = TRUE,
  extra_args = NULL,
  dir = tempdir()
)
}
\arguments{
\item{snp_file}{character. Path to genotype file in BLUPF90 format.}

\item{ped_file}{character or NULL. Path to pedigree file
(animal, sire, dam — space-separated).}

\item{map_file}{character or NULL. Path to SNP map file
(SNP_ID, CHR, POS header).}

\item{call_rate_markers}{numeric. Minimum call rate for markers
(default 0.90). Must be between 0 and 1.}

\item{call_rate_animals}{numeric. Minimum call rate for animals
(default 0.90). Must be between 0 and 1.}

\item{maf}{numeric. Minimum allele frequency threshold (default 0.05).
Must be between 0 and 0.5.}

\item{hwe}{numeric or NULL. HWE deviation threshold. NULL skips the
check (default NULL).}

\item{check_parentage}{logical. Check Mendelian inconsistencies.
Defaults to TRUE when \code{ped_file} is provided, FALSE otherwise.}

\item{trio}{logical. Use sire-dam-progeny trio for Mendelian checks
(default FALSE).}

\item{check_identity}{logical or character. Check for duplicate samples.
If character, path to a file of IDs to compare against (default FALSE).}

\item{identity_threshold}{numeric. Threshold for identity detection
(default 0.99).}

\item{sex_chr}{integer vector or NULL. Chromosome IDs for sex chromosomes.}

\item{skip_chr}{integer vector or NULL. Chromosomes to skip in QC.}

\item{exclude_chr}{integer vector or NULL. Chromosomes to remove entirely.}

\item{save_clean}{logical. Save cleaned genotype files with unqualified
markers/animals removed (default TRUE).}

\item{save_log}{logical. Save QC log file (default TRUE).}

\item{outcallrate}{logical. Output detailed call rate per marker and
animal (default FALSE).}

\item{remove_markers}{logical. Remove unqualified markers progressively
during QC steps (default TRUE).}

\item{remove_animals}{logical. Remove unqualified animals progressively
(default TRUE).}

\item{extra_args}{character vector or NULL. Additional command-line
arguments passed directly to QCF90. Use for options not covered by
other parameters (e.g., \code{c("--no-check-format", "--fastread")}).}

\item{dir}{character. Working directory for output files.}
}
\value{
A list with:
  \describe{
    \item{log}{character vector of QC log content}
    \item{status}{character vector of QC status file content}
    \item{clean_snp}{path to cleaned SNP file (if save_clean)}
    \item{clean_xref}{path to cleaned XrefID file (if present)}
    \item{removed_snp}{character vector of removed SNP info}
    \item{removed_animals}{character vector of removed animal info}
    \item{output}{character vector of program stdout}
  }
}
\description{
Runs QCF90 on genotype and optionally pedigree data to perform
quality checks including call rate filtering, MAF filtering,
monomorphic marker removal, Hardy-Weinberg equilibrium testing,
Mendelian conflict detection, and duplicate sample identification.
}
\details{
QCF90 works with non-renumbered (raw alphanumeric) files, making it
suitable as a first-pass diagnostic before \code{\link{renumf90}}.
}
