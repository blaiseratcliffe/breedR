% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/renumf90.R
\name{build_renumf90_parfile}
\alias{build_renumf90_parfile}
\title{Build RENUMF90 parameter file content}
\usage{
build_renumf90_parfile(
  datafile,
  traits,
  residual_variance,
  effects,
  fields_passed,
  weights,
  snp_file,
  ped_depth,
  inbreeding,
  upg_type,
  gen_int,
  skip_header,
  options
)
}
\arguments{
\item{datafile}{path to the raw data file (space-separated, no header
unless \code{skip_header} is used).}

\item{traits}{integer vector. Column positions of trait(s) in the data file.}

\item{residual_variance}{numeric matrix. Residual (co)variance matrix
(n_traits x n_traits).}

\item{effects}{list of effect specifications. Each element is a list with:
\describe{
  \item{pos}{integer vector of column positions (one per trait, 0 = missing)}
  \item{type}{character: "cross" or "cov"}
  \item{form}{character: "alpha" or "numer" (for cross-classified only)}
  \item{random}{character: "animal", "diagonal", or NULL for fixed}
  \item{optional}{character vector: "pe", "mat", "mpe" (animal effects only)}
  \item{file}{character: pedigree filename (animal/sire effects only)}
  \item{file_pos}{integer vector: animal, sire, dam, altdam, yob positions
    in pedigree file (default c(1,2,3,0,0))}
  \item{covariances}{numeric matrix: (co)variances for this random effect}
  \item{covariances_pe}{numeric matrix: PE (co)variances (if "pe" in optional)}
  \item{covariances_mpe}{numeric matrix: MPE (co)variances (if "mpe" in optional)}
  \item{nested}{list with pos and form for nested covariates}
}}

\item{fields_passed}{integer vector. Columns passed through without
renumbering (or NULL).}

\item{weights}{integer vector. Column position(s) for weights (or NULL).}

\item{snp_file}{character. Path to SNP genotype file (optional).}

\item{ped_depth}{integer. Pedigree search depth (0 = all, default 3).}

\item{inbreeding}{character. Inbreeding method: "pedigree" (default),
"no-inbreeding", or "file <filename>".}

\item{upg_type}{character. Unknown parent group type: NULL (none), "yob",
"in_pedigrees", "group", "group_unisex".}

\item{gen_int}{numeric vector of length 3: min, avg, max generation interval.}

\item{skip_header}{integer. Number of header lines to skip in data file.}

\item{options}{character vector. Additional OPTION lines for RENUMF90.}
}
\value{
Character vector of parameter file lines.
}
\description{
Build RENUMF90 parameter file content
}
